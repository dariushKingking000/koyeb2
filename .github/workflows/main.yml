name: Delete Screenshots

on:
  workflow_dispatch:

jobs:
  clean:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    
    - name: Delete screenshots
      run: |
        rm -rf screenshots/*
        echo "✅ پوشه screenshots خالی شد"
