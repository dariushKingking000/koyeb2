name: 🎥 Screen Record Bot
on: 
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  record:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    
    - name: Install deps
      run: |
        sudo apt update -y
        sudo apt install -y xvfb ffmpeg
        
    - name: Setup Node
      uses: actions/setup-node@v4
      with:
        node-version: 20
    
    - name: Install packages & Create record.cjs
      run: |
        mkdir -p record
        rm -rf node_modules package*.json
        npm init -y
        npm install puppeteer@latest
        echo '{"type": "commonjs"}' > package.json
        
        cat > record/record.cjs << 'JS_EOF'
        const puppeteer = require('puppeteer');
        (async () => {
          try {
            const instructions = ['[click 200 300]'];
            console.log(`📋 ${instructions.length} instructions loaded`);
            
            const browser = await puppeteer.launch({
              headless: false,
              args: ['--no-sandbox', '--disable-dev-shm-usage', '--disable-gpu']
            });
            const page = await browser.newPage();
            await page.setViewport({ width: 1366, height: 768 });
            await page.goto('https://chatgpt.com', { waitUntil: 'networkidle0' });
            await new Promise(r=>setTimeout(r,5000));
            console.log('🌐 ChatGPT loaded');
            
            console.log('🎬 Final wait...');
            await new Promise(r=>setTimeout(r, 8000));
            await browser.close();
            console.log('✅ Automation Complete!');
          }catch(e){
            console.error('❌ Error:', e.message);
          }
        })();
        JS_EOF
    
    - name: Record Screen
      run: |
        rm -rf record/*.mp4
        Xvfb :99 -screen 0 1366x768x24 -ac &
        export DISPLAY=:99
        sleep 5
        
        timeout 240s ffmpeg -f x11grab -s 1366x768 -r 30 -i :99.0 \
          -t 60 -c:v libx264 -pix_fmt yuv420p -crf 23 -preset ultrafast \
          -movflags +faststart -f mp4 ./record/demo_full.mp4 &
        
        FFMPEG_PID=$!
        sleep 5
        node record/record.cjs
        sleep 10
        kill $FFMPEG_PID 2>/dev/null || true
        pkill -f ffmpeg || true
        sleep 5
        
        # تعمیر MP4
        if [ -f ./record/demo_full.mp4 ]; then
          ffmpeg -y -i ./record/demo_full.mp4 -c copy -movflags +faststart ./record/demo_fixed.mp4
          mv ./record/demo_fixed.mp4 ./record/demo_full.mp4
        fi
        
        ls -la record/
        du -sh record/*.mp4
    
    - name: Upload Video Artifact
      uses: actions/upload-artifact@v4
      with:
        name: 🎥-ChatGPT-Demo
        path: record/
        retention-days: 30
    
    - name: Commit & Push Video
      run: |
        git config --global user.name "GitHub Action"
        git config --global user.email "action@github.com"
        git add record/*.mp4 || echo "No video"
        if git diff --staged --quiet; then
          echo "✅ Video in artifacts"
        else
          git commit -m "🎥 ChatGPT demo [skip ci]"
          git push
        fi
