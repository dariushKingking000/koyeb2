- name: Create script
  run: |
    cat > type.js <<'EOF'
const puppeteer=require('puppeteer-extra'),StealthPlugin=require('puppeteer-extra-plugin-stealth');puppeteer.use(StealthPlugin()),(async()=>{const b=await puppeteer.launch({headless:!1,executablePath:'/opt/hostedtoolcache/chromium/latest/x64/chrome',args:['--no-sandbox','--disable-setuid-sandbox']}),p=await b.newPage();await p.goto('https://chatgpt.com'),await p.screenshot({path:'1.png'}),await p.mouse.click(134,254),await p.keyboard.type('سلام! چطوری؟'),await p.screenshot({path:'2.png'}),await b.close()})();
EOF
